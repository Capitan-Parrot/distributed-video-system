postgres:
  dsn: "host=localhost port=5433 user=vidanalytics password=secret dbname=vidanalytics sslmode=disable"

minio:
  endpoint: "localhost:9000"
  access_key: "minio-access-key"
  secret_key: "minio-secret-key"

kafka:
  brokers:
    - "localhost:9091"
  group_id: "video-runner-group4"
  scenario_topic: "video-scenarios"
  heartbeat_topic: "heartbeats"

detection:
  endpoint: "http://localhost:8004"
